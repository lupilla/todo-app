Instructions to run the application:
You nedd only to follow two simple steps to run this project:

1- Run the command to install (if needed) all the dependencies with "yarn install" in the root of the project.
    Dependencies are just installed.
2- To run the application just write "yarn serve"

If you want to run unit tests you must write the following command: "yarn test:unit"

That´s all.